{
  "version": 2,
  "buildCommand": "cp -r frontend .vercel/output/static || mkdir -p .vercel/output/static && cp -r frontend/* .vercel/output/static/",
  "outputDirectory": ".vercel/output/static"
}

